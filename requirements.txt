# AI-CA Minimal Requirements - Production Runtime Dependencies
# Core Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Data Processing
pandas>=2.0.0
duckdb>=0.10.0
pyarrow>=14.0.0
openpyxl>=3.1.0
numpy>=1.24.0

# Vector Databases
qdrant-client>=1.7.0
chromadb>=0.4.0

# LLM & LangChain
langchain>=0.1.0
langchain-core>=0.1.0
langchain-google-genai>=1.0.0
langchain-groq>=0.1.0
langchain-openai>=0.1.0
langchain-huggingface>=0.0.1
langchain-community>=0.0.20

# Caching & IPC
redis>=5.0.0
pyzmq>=25.0.0
upstash-redis>=1.0.0

# AWS Cloud Storage & Services
boto3>=1.34.0
botocore>=1.34.0

# HTTP & Web Scraping
httpx>=0.26.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0

# SQL Validation
sqlparse>=0.4.4

# Utilities
tenacity>=8.2.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
pyyaml>=6.0.0
python-json-logger>=2.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# PyTorch with CUDA 13.0 Support
# Install with: pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu130
# Note: For CPU-only, use: pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# E2B Remote Sandbox (for secure remote code execution)
# Falls back to local sandbox if E2B unavailable
# Enable by setting ENABLE_E2B=true and E2B_API_KEY in environment
e2b-code-interpreter>=0.0.10

# Optional: Ollama/Local LLM (install separately)
# ollama (system binary)
