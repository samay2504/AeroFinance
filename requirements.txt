# AI-CA Production Requirements - Minimal Runtime Dependencies
# Updated: February 2026

# =============================================================================
# CORE WEB FRAMEWORK
# =============================================================================
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# =============================================================================
# DATA PROCESSING & SQL ENGINE
# =============================================================================
pandas>=2.0.0
duckdb>=0.10.0
pyarrow>=14.0.0
openpyxl>=3.1.0
xlrd>=2.0.0
numpy>=1.24.0
sqlparse>=0.4.4

# =============================================================================
# VECTOR DATABASES
# =============================================================================
qdrant-client>=1.7.0
chromadb>=0.4.0

# =============================================================================
# LLM PROVIDERS & LANGCHAIN
# =============================================================================
langchain>=0.1.0
langchain-core>=0.1.0
langchain-google-genai>=1.0.0
langchain-groq>=0.1.0
langchain-openai>=0.1.0
langchain-huggingface>=0.0.1
langchain-community>=0.0.20

# =============================================================================
# CACHING & IPC
# =============================================================================
redis>=5.0.0
upstash-redis>=1.0.0
pyzmq>=25.0.0

# =============================================================================
# AWS CLOUD SERVICES (S3 Storage, ElastiCache)
# =============================================================================
boto3>=1.34.0
botocore>=1.34.0

# =============================================================================
# HTTP & WEB SCRAPING
# =============================================================================
httpx>=0.26.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0

# =============================================================================
# NLP & SEMANTIC UNDERSTANDING
# =============================================================================
# spaCy for semantic routing, NER, and similarity matching
spacy>=3.7.0

# Sentence Transformers for L3 semantic cache and embeddings
sentence-transformers>=2.2.0

# Fuzzy string matching for typo tolerance and column name matching
rapidfuzz>=3.0.0

# =============================================================================
# UTILITIES
# =============================================================================
tenacity>=8.2.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
python-json-logger>=2.0.0

# =============================================================================
# TESTING
# =============================================================================
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# =============================================================================
# PYTORCH (GPU Acceleration for Embeddings)
# =============================================================================
# CUDA 12.1 Support (recommended for modern GPUs):
#   pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# 
# CPU-only (fallback):
#   pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
torch>=2.0.0
torchvision>=0.15.0

# =============================================================================
# PDF PROCESSING & VISION PIPELINE
# =============================================================================
pdfplumber>=0.10.0
Pillow>=10.1.0

# Vision APIs (Google Cloud Vision: 1K units/month free)
google-cloud-vision>=3.7.0
google-generativeai>=0.7.0

# Search & Retrieval (BM25 for keyword pre-filter)
rank-bm25>=0.2.2

# OCR — Local CPU Fallback (zero cost, no API keys)
rapidocr-onnxruntime>=1.3.0     # L3a: ONNX-based, preferred local OCR
pytesseract>=0.3.10             # L3b: Tesseract wrapper, backup fallback

# =============================================================================
# OPTIONAL: REMOTE CODE EXECUTION SANDBOX
# =============================================================================
# E2B for secure remote Python execution (100 hrs/month free)
# Enable: ENABLE_E2B=true, E2B_API_KEY=<key>
# Falls back to local restricted sandbox if disabled
e2b-code-interpreter>=0.0.10

# =============================================================================
# POST-INSTALL: NLP MODELS
# =============================================================================
# After installing requirements, download spaCy language model:
#   python -m spacy download en_core_web_md
#
# For semantic routing, the medium model (300-dim vectors) is required
# Model size: ~40MB download, includes word vectors for similarity matching

